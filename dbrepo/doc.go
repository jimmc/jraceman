/*
Package dbrepo contains an implementation of the domain.Repos interface
that stores data in a database via the database/sql package.
The only jraceman package on which it depends is the domain package.
*/
package dbrepo
